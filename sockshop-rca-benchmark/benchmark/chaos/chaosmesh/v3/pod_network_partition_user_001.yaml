kind: NetworkChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  namespace: sock-shop
  name: pod-network-partition-user-001
spec:
  selector:
    namespaces:
      - sock-shop
    labelSelectors:
      name: user
    pods:
      sock-shop:
        - user-5c64496568-7jpx8
  mode: one
  action: partition
  duration: 180s
  direction: to
