kind: PodChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  namespace: sock-shop
  name: pod-do-fault-carts-db-001
spec:
  selector:
    namespaces:
      - sock-shop
    labelSelectors:
      name: carts-db
  mode: all
  action: pod-failure
  duration: 60s
