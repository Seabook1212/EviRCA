kind: PodChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  namespace: sock-shop
  name: pod-do-fault-queue-master-001
spec:
  selector:
    namespaces:
      - sock-shop
    labelSelectors:
      name: queue-master
    pods:
      sock-shop:
        - queue-master-5bb5566684-9gkdj
  mode: one
  action: pod-failure
  duration: 60s
