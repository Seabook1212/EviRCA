kind: PodChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  namespace: sock-shop
  name: pod-do-fault-orders-db-001
spec:
  selector:
    namespaces:
      - sock-shop
    labelSelectors:
      name: orders-db
  mode: all
  action: pod-failure
  duration: 60s
