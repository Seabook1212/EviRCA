kind: NetworkChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  namespace: sock-shop
  name: pod-network-loss-orders-001
spec:
  selector:
    namespaces:
      - sock-shop
    labelSelectors:
      name: orders
    pods:
      sock-shop:
        - orders-94d48dc8-prbdt
  mode: one
  action: loss
  duration: 180s
  loss:
    loss: '30'
    correlation: '25'
  direction: to
